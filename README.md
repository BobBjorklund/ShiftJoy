# Project Name

Django web app using ASGI (Daphne).

## Local dev
1. Create venv
2. pip install -r requirements.txt
3. python manage.py migrate
4. daphne -b 0.0.0.0 -p $PORT shiftjoy.asgi:application
 doot doot doo

